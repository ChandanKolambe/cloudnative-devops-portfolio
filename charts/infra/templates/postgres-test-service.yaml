apiVersion: v1
kind: Service
metadata:
  name: db-test
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: postgres-test
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
